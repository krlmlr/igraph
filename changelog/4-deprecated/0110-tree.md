# igraph_tree(), igraph_kary_tree()

The deprecated `igraph_tree()` was removed. Use `igraph_kary_tree()` instead.

---

**Original changelog wording:**

> The deprecated `igraph_tree()` was removed. Use `igraph_kary_tree()` instead.
