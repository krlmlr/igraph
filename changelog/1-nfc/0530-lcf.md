# igraph_lcf() and 5 others

**Category**: Graph generators

`igraph_lcf()` was renamed to `igraph_lcf_small()` and `igraph_lcf_vector()` was renamed to `igraph_lcf()`. Now `igraph_lcf()` takes shifts as a vector input, while `igraph_lcf_small()` accepts a shorthand notation where shifts are given as a variable number of function arguments.

---

**Original changelog wording:**

> `igraph_lcf()` was renamed to `igraph_lcf_small()` and `igraph_lcf_vector()` was renamed to `igraph_lcf()`. Now `igraph_lcf()` takes shifts as a vector input, while `igraph_lcf_small()` accepts a shorthand notation where shifts are given as a variable number of function arguments.

---

## Proof of work: `git diff --numstat HEAD..next`

Before and after the change, side by side (add-before, del-before, add-after, del-after, file):

```
add-b  del-b  add-a  del-a  file
  11     0     0     0  changelog/1-nfc/0530-lcf.md
  11    20     7    16  include/igraph_constructors.h
  30    30     0     1  src/constructors/lcf.c
   2    41     1    39  src/constructors/regular.c
  52    54    52    54  doc/generators.xxml
   4     3     3     2  tests/unit/coloring.c
  69     0     0     0  tests/unit/igraph_lcf.c
  28    61     0     0  examples/simple/igraph_lcf.c
   3     0     0     0  examples/simple/igraph_lcf.out
  29    16    28    16  tests/CMakeLists.txt
```

Notes on remaining differences:

- `changelog/1-nfc/0530-lcf.md`: After the change, this file is fully ported. The `del-a` will increase once this proof-of-work section is added (since `next` does not have this section).
- `include/igraph_constructors.h`: Remaining diff (7/16) is from other unrelated changes in this file: license header updates, `__BEGIN_DECLS`→`IGRAPH_BEGIN_C_DECLS`, removal of deprecated `igraph_lattice` and `igraph_tree` declarations, and minor const qualifier changes.
- `src/constructors/lcf.c`: Essentially fully resolved (0/1). The 1-line deletion remaining is from the license header format (`/* -*- mode: C -*-  */` line present in `main-dev` but not in `next`).
- `src/constructors/regular.c`: Large remaining diff (1/39) from other unrelated changes not part of this entry.
- `doc/generators.xxml`: Unchanged (52/54) — many other structural changes in this file belong to other changelog entries.
- `tests/unit/coloring.c`: Remaining diff (3/2) from license header change and `igraph_bipartite_game_gnm` call change (a different changelog entry).
- `tests/unit/igraph_lcf.c`: Fully resolved (0/0).
- `examples/simple/igraph_lcf.c`: Fully resolved (0/0).
- `examples/simple/igraph_lcf.out`: Fully resolved (0/0).
- `tests/CMakeLists.txt`: Remaining diff (28/16) from other test additions/removals belonging to different changelog entries.
