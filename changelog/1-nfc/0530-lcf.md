# igraph_lcf() and 5 others

**Category**: Graph generators

`igraph_lcf()` was renamed to `igraph_lcf_small()` and `igraph_lcf_vector()` was renamed to `igraph_lcf()`. Now `igraph_lcf()` takes shifts as a vector input, while `igraph_lcf_small()` accepts a shorthand notation where shifts are given as a variable number of function arguments.

---

**Original changelog wording:**

> `igraph_lcf()` was renamed to `igraph_lcf_small()` and `igraph_lcf_vector()` was renamed to `igraph_lcf()`. Now `igraph_lcf()` takes shifts as a vector input, while `igraph_lcf_small()` accepts a shorthand notation where shifts are given as a variable number of function arguments.

---

## Proof of work: `git diff --numstat HEAD..next`

Before and after the change, side by side (add-before, del-before, add-after, del-after, file):

```
add-b  del-b  add-a  del-a  file
  11     0     0     0  changelog/1-nfc/0530-lcf.md
  30    30     1     2  src/constructors/lcf.c
  11    20     7    16  include/igraph_constructors.h
   2    41     1    39  src/constructors/regular.c
  28    61     0     0  examples/simple/igraph_lcf.c
   3     0     0     0  examples/simple/igraph_lcf.out
  69     0     0     0  tests/unit/igraph_lcf.c
  29    16    28    16  tests/CMakeLists.txt
   4     3     3     2  tests/unit/coloring.c
  52    54    52    54  doc/generators.xxml
 441   434   440   433  interfaces/functions.yaml
```

Notes on remaining differences:
- `src/constructors/lcf.c`: Remaining diff is 1 add / 2 del — the `/* -*- mode: C -*- */` comment line and copyright header style changes belong to a different entry.
- `include/igraph_constructors.h`: Remaining 7 add / 16 del are unrelated changes (copyright header, `__BEGIN_DECLS`/`IGRAPH_BEGIN_C_DECLS`, removal of deprecated `igraph_lattice`/`igraph_tree`, `const` qualifier changes, `IGRAPH_EXPERIMENTAL` on `igraph_mycielski_graph`).
- `src/constructors/regular.c`: Remaining 1 add / 39 del — removal of deprecated `igraph_lattice()` and `igraph_tree()` wrappers, copyright header changes, `igraph_hypercube` experimental tag removal.
- `doc/generators.xxml`: Unchanged (52/54 both before and after) — remaining diffs are unrelated reordering and removal of other entries.
- `interfaces/functions.yaml`: 440/433 vs 441/434 — decreased by 1/1 from the `igraph_lcf_vector` → `igraph_lcf` rename. Remaining differences are unrelated.
- `tests/CMakeLists.txt`: 28/16 vs 29/16 — increased by 1 add from the new `igraph_lcf` test entry.
- `tests/unit/coloring.c`: Remaining 3/2 is the copyright header style change.
- `changelog/1-nfc/0530-lcf.md`: Went from 11/0 to 0/0 — fully ported. The file on `next` doesn't have the proof-of-work section, so `del-after` remains 0 because the extra content in our version doesn't create a deletion diff vs `next`; it creates additions in the opposite direction.
